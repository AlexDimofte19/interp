okay so far the analysis i did was on the test set. This is not good. I need to apply the jlens on to the train set, then use my script to extract the direction-rich token positions, then modify gather activations so that it extracts exact positions i offer, then modify probe train to use all the activations in the dataset (i think no modification needed). Make sure probe train is done on actions. So tasks are the following:
0. check if i need to re-fit the jlens. I have a suspicion it was trained on the test set [ checked, trained on training set thank god]
1. apply jlens on train dataset
	1.1 modify script to create a csv file for each trajectory, which then follows the folder structure of the trajectories
	1.2 modify script to also extract activations and save them (gather activations already used here, just expand to extract all activations, perhaps from layers 7 to 20.
	1.3 The outputting structure should be path/to/activations/activations_folder/size/trajectory_name/openai__gpt-oss-20b/layer_n/step_n/reasoning/*.pt for the saved activations. Meanwhile, the results of the jlens being applied should be in path/to/activations/activations_folder/size/trajectory_name/openai__gpt-oss-20b/j_lens_analysis.csv, and this should include information about all the tokens in this trajectory at each step and at each layer mentioned.

2.  By this point I will have activations, and corresponding csvs. We must focus now on training a probe - how?
	2.1 randomly selected reasoning tokens
		2.1.1 randomly selected tokens & randomly selected layers?
		2.1.2 randomly selected tokens & layer 15 using traditional wisdom
	2.2 j-lens selected reasoning tokens
		2.2.1 select tokens with richest directional data in their j-space
		2.2.2 for each token, select top M layer(s) with the richest directional information (check with gabriele, how many)
		2.2.3 make new dataset by copying the activations to separate folder in path/to/activations/activations_folder_jlens_selection which follows the same structure. No need for the csvs now
		2.2.4 make sure probe train can use all tokens
		

3. Evaluation - by this point we will have the probes. Some methods to brainstorm with gabriele:
	3.1 Tokens selection:
		3.1.1 use all the tokens in the test set
		3.1.2 use randomly selected tokens in the test set
		3.1.3 use jlens selected tokens
	3.2 Layer selection:
		3.2.1 go through each layer
		3.2.2 conventional wisdom - layer 15
		3.2.3 jlens selected layers

4. Activation patching - if we use jlens to locate directionally rich tokens, can we also modify the jspace to affect results - is this necessary? we already know from anthropic that this would be the case
	do we need to do activation patching using the probe itself?

